{
  "name": "give-google-analytics",
  "private": true,
  "scripts": {
    "start": "npm install && npm run watch",
    "dev": "mix",
    "production": "mix --production",
    "build": "mix --production",
    "watch": "mix watch"
  },
  "devDependencies": {
    "laravel-mix": "^6.0.49"
  }
}
